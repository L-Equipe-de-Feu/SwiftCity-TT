#ifndef MAP_H
#define MAP_H

#include <iostream>
#include <math.h>
#include <string.h>
#include <random>
#define m 64
#define n 64

//random_device r;
//default_random_engine generator(r());

using namespace std;

void randomized_map(int x, int map[m][n]);

#endif
